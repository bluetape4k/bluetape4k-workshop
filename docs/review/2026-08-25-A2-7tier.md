# A2 assertions ecosystem 재사용 7-Tier review

## 검토 범위와 판정

<!-- reviewed_implementation_oid: 0912ee52467ed9eb83c918090d5eda40f8f1e8d0 -->

이 문서는 `#792` Epic의 A2 stacked child인 `test/ecosystem-reuse-assertions-platform`에서
manifest에 등록된 #785~#791 테스트 앵커 9개와 이 review artifact만 검토한다.
현재 rebase 후 구현 ancestor는 `0912ee52467ed9eb83c918090d5eda40f8f1e8d0`이며
parent/base는 `develop@e69c421adaee0dafbafc352d0efac37a379798c7`다. A2 PR
#829는 exact head `3efb5870e3ec7b754b0f9fc678c1bc56a8f4ad48`로 rebase merge되어
`323290adec9e8904f566ed736217369d78313896`에 통합되었다. serial closeout가 이
implementation OID를 manifest와 hosted PR evidence에 결속했다. 이 lane에서는
직접 GitHub PR/merge/push를 실행하지 않고 coordinator가 delivery gate를 소유한다.

결론은 **로컬 구현 PASS, P0=0, P1=0, P2=1, P3=0**이다. 테스트 앵커의
plain assertion 재사용은 완료했으며, module test 9개를 순차 실행해 907개가
통과했다. P2는 #785의 production logging anchor가 현재 A2 allowlist에 없어서
이 test-only lane에서 다루지 못한 범위 기록이다. 따라서 이 문서는 #785 전체
해결 또는 A2 hosted `READY`/`MERGE_READY`를 선언하지 않는다.

## 변경 파일

| 파일 | 변경 | 판정 |
|---|---|---|
| `commerce/concert-ticket-flash-sale/src/test/kotlin/io/bluetape4k/workshop/commerce/ticket/web/TicketBrowserContractTest.kt:3-34` | HTML, JavaScript, CSS 계약과 classpath resource 존재 확인을 `shouldContain`, `shouldNotContain`, `shouldBeTrue`로 변경했다. 접근성·보안·cache 문자열은 그대로 유지했다. | PASS |
| `operations/job-console-spring/src/test/kotlin/io/bluetape4k/workshop/operations/jobconsole/spring/JobConsoleSpringSubmissionHttpTest.kt:3-60` | 네 가지 예외 검증을 JUnit `assertThrows`에서 Bluetape `assertFailsWith`로 변경했다. Spring request/JSON/HTTP 경계는 변경하지 않았다. | PASS |

나머지 manifest 앵커 7개는 이미 Bluetape assertions를 사용하고 있어 불필요한
수정을 하지 않았다.

| 앵커 | 기존 확인 |
|---|---|
| `aws/kinesis-coroutines/src/test/kotlin/io/bluetape4k/workshop/aws/kinesis/KinesisStreamServiceTest.kt` | `assertFailsWith`, `shouldBeEqualTo` 사용; raw assertion 없음 |
| `commerce/promotion-voucher-campaign/src/test/kotlin/io/bluetape4k/workshop/commerce/voucher/VoucherEndToEndIntegrationTest.kt` | `assertFailsWith`, `shouldBeEqualTo`, `shouldBeTrue` 사용; raw assertion 없음 |
| `commerce/event-sourced-promotion-voucher-campaign/src/test/kotlin/io/bluetape4k/workshop/commerce/voucher/eventsourced/EventSourcedRuntimeContractTest.kt` | `shouldBeEqualTo`, `shouldBeTrue`, `shouldNotBeNull`, `shouldNotContain` 사용; raw assertion 없음 |
| `commerce/pre-generated-voucher-pool/src/test/kotlin/io/bluetape4k/workshop/commerce/voucherpool/fixture/VoucherPoolFixturesTest.kt` | `assertFailsWith`, `shouldBeEqualTo`, `shouldBeFalse`, `shouldBeTrue` 사용; raw assertion 없음 |
| `operations/job-console-core/src/test/kotlin/io/bluetape4k/workshop/operations/jobconsole/api/JobApiModelsTest.kt` | `shouldBeEqualTo` 사용; raw assertion 없음 |
| `operations/job-console-ktor/src/test/kotlin/io/bluetape4k/workshop/operations/jobconsole/ktor/JobConsoleKtorSubmissionHttpTest.kt` | `shouldBeEqualTo` 사용; Ktor route/status API 보존; raw assertion 없음 |
| `optimization/planning-contracts/src/test/kotlin/io/bluetape4k/workshop/optimization/planning/OptimizationRuntimeContractTest.kt` | `shouldBeEqualTo`, `shouldBeTrue` 사용; raw assertion 없음 |

## #785 범위 결정

`KinesisStreamServiceTest.kt`는 manifest의 승인된 A2 test anchor이며 기존부터
Bluetape assertions를 사용하므로 소스 변경이 필요하지 않았다. Issue #785가
지정한 production anchors인 `KinesisDemoRunner.kt`와
`KinesisShutdownConfiguration.kt`는 현재 A2 `allowed_paths`에 없다. 따라서
lazy logging 또는 `KLoggingChannel` production 변경을 이 branch에 넣지
않았으며, 해당 production logging closure는 별도 승인된 scope에서 다시
검증해야 한다. 이 경계를 지키기 위해 production code와 manifest를 수정하지
않았다.

## 7-Tier 결과

| Tier | 판정 | 근거 |
|---|---|---|
| 1. 의미·범위 | PASS | 변경은 manifest의 두 잔여 plain assertion 앵커에만 적용했다. browser contract 문자열, Spring request/JSON protocol, Ktor route API의 의미는 유지했다. |
| 2. 정확성·API | PASS | `assertFailsWith<T>`가 기존 네 가지 예외 타입을 그대로 검증하고, 문자열 계약은 의도 기반 `shouldContain`/`shouldNotContain`으로 보존된다. 새 public API나 dependency는 없다. |
| 3. lifecycle·동시성 | N/A/PASS | assertion helper만 바뀌어 lifecycle/concurrency 동작은 추가하지 않았다. Kinesis와 voucher 계열 기존 module test를 순차 실행했고 lifecycle 관련 회귀는 없었다. |
| 4. 보안·경계 | PASS | `innerHTML`, `localStorage`, `document.cookie` 금지 계약과 `textContent`, `cache: 'no-store'` 계약을 그대로 검사한다. 인증·비밀·외부 신뢰 경계는 변경하지 않았다. |
| 5. 성능·자원 | N/A | production hot path, fixture, container, dependency graph를 변경하지 않았다. 전체 selector는 `--max-workers=1 --no-parallel`로 실행했다. |
| 6. 테스트·정적 분석 | PASS | 9개 manifest selector에서 총 907개 테스트가 통과했고, targeted 재실행·root `detekt`·raw assertion scan·`git diff --check`가 통과했다. |
| 7. 문서·운영 | PASS (로컬) | 이 artifact에 scope, fallback, #785 경계, checklist, exact verification을 기록했다. hosted read-back, receipt/state 전이, PR 및 rebase merge는 coordinator 범위로 남아 있다. |

## Assertion 재사용과 의도적 fallback

9개 앵커 전체를 다음 금지 패턴으로 재검사한 결과 일치 항목은 0건이다.

```text
org.junit.jupiter.api.assert*, kotlin.test.*, assertEquals, assertNotEquals,
assertThrows, assertTrue, assertFalse, assertNull, assertNotNull, line-leading check(
```

이번 변경에서는 raw assertion fallback을 남기지 않았다. `TicketBrowserContractTest`의
정적 resource 문자열은 테스트 대상 동작 계약이므로 Bluetape `shouldContain`/
`shouldNotContain`으로 표현했다. Spring의 `MockHttpServletRequest` 구성과
Ktor의 route/status API는 framework/protocol surface이며 assertion fallback이
아니므로 그대로 보존했다. 기존 Kinesis/voucher/operations/planning matcher도
provider-gap 없이 유지했다.

## Kotlin pattern checklist

`$bluetape-kotlin-patterns`의 `references/testing.md`,
`references/checklist.md`, `references/spring-boot.md`를 확인했다. Spring
reference는 auto-configuration/property/conditional bean 변경에 적용되는
규칙이며, 이번 변경은 `MockHttpServletRequest` 테스트 assertion 교체만
수행하므로 configuration checklist는 N/A다.

| 체크 | 판정 | 증거 |
|---|---|---|
| KT-01 — Kotlin/test pattern 로드 | PASS | Kotlin pattern의 testing/checklist와 Spring 적용 범위를 읽고 대상 테스트에 적용했다. |
| KT-02 — 생태계 재사용/BOM | PASS | 기존 `io.github.bluetape4k.assertions` API와 root BOM을 재사용했다. build/catalog/dependency 선언은 변경하지 않았다. |
| KT-03 — 안전성·경계 | PASS | production 코드, cancellation, lifecycle, protocol, fixture를 변경하지 않았고 새 `!!`/무제한 동작을 추가하지 않았다. |
| KT-04 — 검증 | PASS | targeted/full module test, root detekt, raw scan, diff check를 fresh 실행했다. |
| KT-05 — A2 scope/DoD | PASS | manifest-listed 9 anchors와 허용된 review artifact만 변경했고 P0/P1 blocker가 없다. |
| KT-TEST-01 — assertion 의미 보존 | PASS | exact exception type와 HTML/JS/CSS contract를 Bluetape matcher로 검증한다. |
| KT-TEST-02 — coroutine/concurrency | N/A | 새 coroutine/concurrency behavior가 없다. |
| KT-TEST-03 — container/fixture | N/A | 새 container/fixture가 없다. 기존 full module tests만 실행했다. |
| KT-TEST-04 — HTTP adapter | N/A | HTTP adapter/factory 구현 변경이 없고 Spring/Ktor test protocol setup을 보존했다. |
| KT-TEST-05 — 실행 증거 | PASS | 아래 9개 selector, targeted 재실행, detekt 결과가 모두 fresh PASS다. |

A2 leaf-local required checks는 **7/7 PASS**, N/A는 **3**, blocked는 **0**이며
unchecked 항목은 없다. Workflow/manifest state, hosted CI, PR metadata와 merge
approval은 이 local implementation artifact의 required checks가 아니며
coordinator가 별도로 완료해야 한다.

## P0/P1/P2/P3 review

| 심각도 | 수 | 내용 |
|---|---:|---|
| P0 | 0 | 없음 |
| P1 | 0 | 없음 |
| P2 | 1 | #785 production lazy logging anchors가 A2 allowlist 밖이므로 이 lane에서 미수정이다. Issue 전체 closure 전 해당 scope의 별도 승인·검증이 필요하다. |
| P3 | 0 | 없음 |

P2는 현재 변경의 correctness blocker가 아니라 manifest scope와 issue closure
사이의 추적 항목이다. 이를 이유로 production 파일을 이 branch에 몰래 추가하지
않았다.

## 의존성 및 검증 영수증

의존성 선언은 변경하지 않았으며 `dependencyInsight`에서 assertions가 기존
consumer BOM 경로로 수렴함을 확인했다.

```text
io.github.bluetape4k:bluetape4k-assertions:1.12.1
  <- io.github.bluetape4k:bluetape4k-bom:1.12.1
  <- io.github.bluetape4k:bluetape4k-dependencies:1.4.0
```

사용한 Gradle 공통 옵션은 `--no-daemon --no-build-cache --max-workers=1
--no-parallel`이다.

| 검증 | 결과 |
|---|---|
| `:aws-kinesis-coroutines:test` | PASS — 48 tests |
| `:commerce-promotion-voucher-campaign:test` | PASS — 188 tests |
| `:commerce-event-sourced-promotion-voucher-campaign:test` | PASS — 63 tests |
| `:commerce-pre-generated-voucher-pool:test` | PASS — 366 tests |
| `:commerce-concert-ticket-flash-sale:test` | PASS — 94 tests |
| `:operations-job-console-core:test` | PASS — 97 tests |
| `:operations-job-console-spring:test` | PASS — 11 tests |
| `:operations-job-console-ktor:test` | PASS — 6 tests |
| `:optimization-planning-contracts:test` | PASS — 34 tests |
| targeted `*TicketBrowserContractTest` | PASS — 1 test |
| targeted `*JobConsoleSpringSubmissionHttpTest` | PASS — 5 tests |
| root `detekt` | PASS — 112 actionable tasks, 0 failures |
| raw assertion scan over 9 anchors | PASS — 0 forbidden matches |
| `git diff --check` | PASS |
| Spring assertions `dependencyInsight` | PASS — resolved `bluetape4k-assertions:1.12.1` |

Full selector commands:

```text
./gradlew :aws-kinesis-coroutines:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :commerce-promotion-voucher-campaign:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :commerce-event-sourced-promotion-voucher-campaign:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :commerce-pre-generated-voucher-pool:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :commerce-concert-ticket-flash-sale:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :operations-job-console-core:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :operations-job-console-spring:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :operations-job-console-ktor:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :optimization-planning-contracts:test --no-daemon --no-build-cache --max-workers=1 --no-parallel
```

Targeted commands:

```text
./gradlew :commerce-concert-ticket-flash-sale:test --tests "*TicketBrowserContractTest" --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :operations-job-console-spring:test --tests "*JobConsoleSpringSubmissionHttpTest" --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew detekt --no-daemon --no-build-cache --max-workers=1 --no-parallel
./gradlew :operations-job-console-spring:dependencyInsight --dependency io.github.bluetape4k:bluetape4k-assertions --configuration testRuntimeClasspath --no-daemon --no-build-cache --max-workers=1 --no-parallel
```

초기 baseline에서 두 Gradle 실행을 동시에 시작했을 때 `shared:compileKotlin`
cache 삭제 경합이 한 번 발생했으나, 이는 source failure가 아닌 실행 경합이었다.
이후 모든 검증은 순차 실행했고 위 결과를 fresh evidence로 사용한다.

## DoD Status

| 계획 항목 | 상태 | 증거 |
|---|---|---|
| 1. AGENTS와 Kotlin/TDD 계약 확인 | COMPLETED | workflow gate, Kotlin pattern references, TDD 절차 확인 |
| 2. #785~#791 manifest 앵커 매핑 | COMPLETED | 9개 allowlisted test anchor와 issue intent 대조 |
| 3. 최소 assertion migration 구현 | COMPLETED | 허용된 2개 Kotlin test 파일만 변경, 7개는 기존 준수 확인 |
| 4. 범위 검증·P0~P3 review·root 보고 | COMPLETED | 이 artifact, 907 tests, detekt, scan, diff check |

현재 local A2 구현 DoD와 child rebase merge는 **DONE**이다. serial closeout가
manifest receipt와 inventory 상태를 반영했으며, A2의 남은 #785 production
logging 범위는 별도 후속으로 유지한다.

### 다음 조치

1. #785 production logging scope를 별도 승인된 child lane에서 검토한다.
2. A2 범위 밖 remaining train tracks는 최신 develop에서 순차 진행한다.

### Post-merge closeout

- PR #829 rebase merge: `323290adec9e8904f566ed736217369d78313896`
- manifest execution receipt: `20260827T102153Z-a2-merge-closeout`
- 현재 train 상태: A2 `READY/PASS`; #785는 `pending`, #787/#791은 fresh anchor
  검증으로 `verified`

## 2026-08-28 후속 검토 — #785~#791

이번 후속 lane은 이전 기록을 덮어쓰지 않고, #785~#791의 현재 구현과 검증
결과를 별도 7-Tier review로 남긴다. 목표는 Kinesis lifecycle logging을
Bluetape logging contract로 정렬하고, framework DSL을 유지한 채 plain
assertion만 Bluetape assertion helper로 교체하는 것이다.

| Tier | 검토 결과 | 증거 및 판단 |
|---|---|---|
| 1. 범위·요구사항 | PASS | #785 Kinesis production logging, #786~#790 plain assertion migration, #791의 기존 A2 ancestor 준수 상태를 확인했다. |
| 2. 구조·경계 | PASS | `KLoggingChannel`을 Kinesis 두 lifecycle 컴포넌트에 적용했고, WebTestClient/Awaitility/WireMock DSL은 유지했다. |
| 3. Kotlin 패턴 | PASS | logging은 lazy lambda를 사용하고, 예외 계약은 `assertFailsWith`, 값·컬렉션 계약은 Bluetape `should*` matcher로 표현했다. |
| 4. 테스트 설계 | PASS | RED 정적 baseline 168건의 legacy assertion/logger hit를 기록한 뒤, GREEN에서 금지된 JUnit assertion import/call과 raw `check`가 0건이 됐다. Kinesis의 `LoggerFactory`/`LOGGER` 6건도 0건이다. |
| 5. 실행 검증 | PASS | 12개 affected module `compileTestKotlin` 성공, targeted test 전부 성공, root `detekt` 성공, `git diff --check` 성공. 상세 결과는 아래 표에 기록한다. |
| 6. 적대적 검토 | PASS | 예외 타입·메시지/레벨·redaction 동작을 보존했고, framework DSL 전환·의존성 추가·manifest 수정·무관한 production cleanup을 하지 않았다. fixture/property precondition인 `requireNotNull` 8건은 plain assertion migration 대상이 아니어서 유지했다. |
| 7. DoD·인계 | PASS | 21개 Kotlin source/test 파일과 본 review artifact만 변경했고 P0/P1 blocker는 없다. PR 생성·승인·merge는 하지 않았으며 최종 train 인계 대상으로 남겼다. |

### RED → GREEN 정적 증거

| 검사 | RED baseline | GREEN 결과 |
|---|---:|---:|
| 지정 대상의 JUnit assertion import/call | 168 legacy hit에 포함 | 0 |
| 지정 대상의 raw `check`/`checkNotNull` assertion | 168 legacy hit에 포함 | 0 |
| Kinesis `LoggerFactory`/`LOGGER` | 6 | 0 |
| 의도적으로 유지한 `requireNotNull` setup/property guard | 8 | 8 (비대상 precondition) |

### Fresh verification

| 검증 | 결과 |
|---|---|
| 12개 affected module `compileTestKotlin` | PASS — `BUILD SUCCESSFUL in 55s` |
| `:aws-kinesis-coroutines:test` | PASS — 48 tests |
| `:commerce-order-lifecycle-fulfillment:test --tests '*LifecyclePoliciesTest'` | PASS — 4 tests |
| `:commerce-usage-metering-billing-ledger:integrationTest` 대상 2개 | PASS — 3 + 4 tests |
| `:commerce-promotion-voucher-campaign:test` 대상 6개 | PASS — 17 tests |
| `:commerce-pre-generated-voucher-pool:test --tests '*VoucherPoolEventStreamIntegrationTest'` | PASS — 9 tests |
| `:commerce-event-sourced-promotion-voucher-campaign:test --tests '*EventSourcedRuntimeContractTest'` | PASS — 3 tests |
| `:commerce-concert-ticket-flash-sale:test --tests '*TicketBrowserContractTest'` | PASS — 1 test |
| `:leader-job-safety-lab:test --tests '*JobRunCoordinatorTest'` | PASS — 8 tests |
| `:operations-job-console-core:test` 대상 3개 | PASS — 12 tests |
| `:operations-job-console-spring:test --tests '*JobConsoleSpringSubmissionHttpTest'` | PASS — 5 tests |
| `:operations-job-console-ktor:test --tests '*JobConsoleKtorSubmissionHttpTest'` | PASS — 1 test |
| `:optimization-planning-contracts:test` planning 대상 | PASS — 34 tests |
| `./gradlew detekt --no-daemon --no-build-cache --max-workers=1 --no-parallel` | PASS — 0 violations |
| `git diff --check` | PASS |

### 범위·manifest 인계 메모

Issue body가 요구하는 대상과 #785 production 변경은 현재 A2 manifest의 stale한
`allowed_paths` 앵커보다 넓다. 부모 train/manifest 소유권을 침범하지 않기 위해
manifest와 inventory는 수정하지 않았으므로, PR 생성 전에 coordinator가 lane
receipt와 allowlist를 현재 issue 범위에 맞춰 조정해야 한다. 이 child lane에서는
PR/merge/approval을 수행하지 않는다.

현재 A2 구현 DoD는 **DONE**이며, parent train의 최종 rebase merge는 **PENDING**이다.
