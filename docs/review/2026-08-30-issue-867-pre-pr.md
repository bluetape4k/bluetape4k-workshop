# Issue #867 pre-PR 통합 리뷰

## 검토 범위

`origin/develop`의 `985beb08a0e16bec92dcd68d17bdb7a2e2b2ffc1`에서
`feat/issue-867-leader-audit-export`가 변경한 `leader/job-safety-lab` audit export
경계를 검토했다. 기준은 live Issue #867, upstream
`bluetape4k-leader` PR #792 merge `440e4f4e65a88eefdb822a5f3c1a7d44cd104046`,
현재 workshop의 BOM·workflow·README 계약이다.

## 독립 관점 수렴

| 관점 | 근거 | P0 | P1 | P2 | P3 | 판정 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| API/developer | upstream public exporter/subscription API, versionless catalog, 기존 elector 주입 호환성 | 0 | 0 | 0 | 0 | PASS |
| Ops/workflow | MEMORY 기본값, operator endpoint, fixed meter, shutdown coordinator, Examples/smoke/stale 등록 | 0 | 0 | 0 | 0 | PASS |
| Stability | queue/drop/retry/cancel, idempotent close, context restart termination | 0 | 0 | 0 | 0 | PASS |
| Performance | bounded queue·payload·history·aggregate reservation, non-blocking admission | 0 | 0 | 0 | 0 | PASS |
| Security | HTTPS exact host allow-list, header allow-list, redaction, actuator show-values | 0 | 0 | 0 | 0 | PASS |
| User/caller | EN/KO README parity, operator role, PostgreSQL authority·exactly-once 제외 고지 | 0 | 0 | 0 | 0 | PASS |

초기 delegated lane에서 발견된 계약 부재와 후속 review의 P1/P2는
`Long` checked arithmetic, serialized payload 단일 경계, transport-independent
recording, local meter catalog, aggregate shutdown, preemptive close watchdog,
scheduler cancellation, README parity로 보완하고 영향 범위를 재검증했다.

## Issue acceptance와 중복성

- Issue #867의 bounded redacted audit, MEMORY 기본 transport, trusted HTTPS opt-in,
  queue/backpressure/retry/cancel/close, operator report, PostgreSQL authority 보존,
  EN/KO README와 저장소 등록 기준을 모두 구현·테스트·문서에 매핑했다.
- 기존 Issue #866의 leader diagnostics 및 기존 job-safety persistence와 겹치지 않으며,
  audit export는 authoritative history·business outbox·exactly-once delivery를
  대체하지 않는다.
- 새 모듈을 만들지 않고 `leader/job-safety-lab`을 확장했다. root
  `bluetape4k-dependencies` BOM만 버전 권위로 사용하며 새 alias는 versionless다.

## 코드·문서·검증

- `AdmissionOnlyLeaderHistorySink`는 PostgreSQL history를 저장하지 않고 lifecycle
  admission key만 제공한다.
- report는 endpoint·authorization·token·raw identity·raw exception을 모델에
  포함하지 않으며, retained payload와 upstream snapshot을 방어적으로 투영한다.
- 하나의 `JobSafetyAuditShutdownCoordinator`가 subscription → exporter → HTTP client
  → scheduler → executor → coroutine scope를 단조 deadline으로 종료한다.
- module README EN/KO, coverage matrix, lesson index/lesson, README validator와
  workflow/stale 계약을 함께 유지했다.
- 공개 GitHub Issue/PR metadata는 `[2.0.0]` prefix, milestone `2.0.0`, 한국어 본문을
  사용한다.

## 검증 결과

- unit: 88 tests, failed 0, skipped 0.
- integration: 12 tests, failed 0, skipped 0.
- `:leader-job-safety-lab:check`: PASS.
- `scripts/smoke-validate.sh leader-full`: PASS.
- `scripts/smoke-validate.sh stale-check`: active modules 131, stale 0,
  required registration missing 0, broken image links 0.
- `node scripts/validate-job-safety-lab-readme.mjs`: 2 locales, 6 scenarios,
  11 states, 8 assets PASS.
- bounded placeholder/public-secret scan과 `git diff --check`: PASS.
- detekt: root task graph에 task가 없어 N/A; module check와 compile/test로 대체 검증.

## Ecosystem scope repair

초기 PR #899의 `Ecosystem Reuse Gate`는 변경 경로가 기존 manifest track에
속하지 않아 `found 0`으로 fail-closed했다. 고정 track을 수정하지 않고 현재
PR의 exact `develop` base와 `feat/issue-867-leader-audit-export` head를 가진
`issue-867-leader-audit` follow-up scope를 추가했으며, `coordinator_scope_receipt`는
기존 receipt를 재사용하지 않은 fresh ID/checksum으로 갱신했다. 이 scope는 이번
PR의 `leader/job-safety-lab`와 audit 문서/검토 경로만 허용한다.

## 판정과 delivery hold

현재 구현·문서·테스트 관점의 통합 판정은 **PASS (P0=0, P1=0, P2=0, P3=0)**다.
실제 외부 audit endpoint delivery는 의도적으로 호출하지 않았고, PR 생성 후 exact head,
live CI required-check, review와 metadata를 다시 읽어야 한다. fresh exact-head
`승인` 전 merge와 auto-merge는 보류한다.

## 최종 리뷰 후 수렴

최종 code review가 지적한 shutdown 예외 무음 처리(P1)를 다음처럼 보완했다.

- `JobSafetyAuditShutdownCoordinator`가 resource step, outcome, exception type만
  구조화 warning으로 기록하고 후속 cleanup을 계속한다.
- shutdown 실패 경로 테스트가 raw exception message 비노출과 resource step 관찰을
  검증한다.
- malformed retained payload는 `malformedPayloadCount`와 제한된 decode warning으로
  관찰한다.
- MEMORY/HTTPS exporter parity 및 32개 동시 submit admission latency 회귀 테스트를
  추가했다.
- 보완 후 targeted audit suite는 12 tests, 0 failed, 0 skipped로 통과했다.

보완된 diff에 대한 후속 최종 review와 원격 CI는 새 exact head에서 다시 확인한다.
