# F1 Field Service 7-Tier code review

## 검토 범위와 판정

F1은 A1 exact head `222a0fda06c36bf3074df789095463b26604f39e`를 base로 하는
`fix/ecosystem-reuse-field-service-contracts` lane이다. 검토 대상은
`optimization/field-service-dispatch`의 outbox redaction, aggregate event CAS,
route/replan concurrency boundary, value-class ID normalization과 그 계약
테스트이며, manifest allowlist 밖의 파일은 변경하지 않는다.

현재 로컬 판정은 **P0=0, P1=0, P2=0, P3=0 / F1 구현 PASS**다. hosted CI,
PR review/thread read-back, merge는 PR 생성 이후의 별도 게이트이므로 아직
`PENDING`이다.

## 7-Tier 결과

| Tier | 확인 내용 | 판정 | 근거 |
|---|---|---|---|
| Tier 1 — Correctness | outbox handler 예외의 안정적 redacted reason, 동일 aggregate expected version의 단일 append, route confirmation의 단일 commit, value-class ID의 trim/검증 보존 | PASS | `FieldServiceOutboxWorkerTest`, `FieldServiceCasIntegrationTest`, `FieldServiceCanonicalizerTest` |
| Tier 2 — Reliability | `CancellationException` 재전파, retry reason의 raw message 비저장, duplicate/event-key reuse 구분, bounded timeout과 task cancellation | PASS | `FieldServiceOutboxWorker`, `FieldServiceRepositories`, `FieldServiceReplanService` 및 56개 모듈 테스트 |
| Tier 3 — Concurrency | `TestingExecutors`와 barrier로 event CAS 및 route confirmation 동시성 검증, JDBC 기준 데이터를 `VirtualThreads.executorService()` 경계로 이동, CPU planner admission/queue 제한 유지 | PASS | `FieldServiceCasIntegrationTest`의 동시성 2건과 virtual-thread boundary 테스트 |
| Tier 4 — Security/Privacy | 예외 raw message와 secret이 retry/outbox에 남지 않음; payload summary는 기존 bounded contract 유지 | PASS | `handler failure stores a stable redacted reason`에서 `secret=do-not-persist` 비저장 확인 |
| Tier 5 — API/Compatibility | `FieldServiceCommand.eventVersion`을 CAS sequence와 분리하고 기본값으로 기존 호출 호환, command 결과에 `VERSION_CONFLICT`를 명시 | PASS | `FieldServiceRecords`, `FieldServiceCommandService`, HTTP adapter branch read-back |
| Tier 6 — Maintainability | 기존 repository/transaction/virtual-thread helper 재사용, 새 dependency·개별 bluetape BOM·중복 wrapper 없음, 한국어 review artifact와 issue traceability 제공 | PASS | dependency receipt, manifest allowlist, source diff |
| Tier 7 — Operations/Verification | RED/GREEN, 전체 모듈 XML 집계, root detekt, dependencyInsight, diff/checker 결과를 F1 worktree에서 재실행 | PASS | 아래 검증 영수증; hosted/merge는 PENDING |

## Kotlin pattern 및 bluetape4k 재사용

- `bluetape4k.assertions`: `shouldBeEqualTo`, `shouldBeTrue`,
  `shouldNotBeNull`을 사용해 상태·널 계약을 검증하며 production/test의
  무분별한 `!!`를 제거했다.
- `bluetape4k.junit5.concurrency.TestingExecutors`: 고정 thread pool과
  bounded barrier로 event append/route confirmation의 원자성을 재현한다.
- `bluetape4k.concurrent.virtualthread.VirtualThreads`: JDBC 기준 데이터 조회만
  virtual-thread executor로 보내고 CPU planner executor의 bounded
  admission은 유지한다.
- `bluetape4k-idgenerators`는 기존 모듈 contract를 그대로 사용하고,
  dependencyInsight에서 `1.12.1`이 `bluetape4k-dependencies:1.4.0` 경유로
  해석됨을 확인했다. `assertions`와 `junit5`도 같은 BOM 경로의 `1.12.1`이다.
- raw `CyclicBarrier`/`Callable`은 동시성 경계 자체를 명시해야 하는 JDK
  primitive라서 사용했다. executor 생성·종료는 `TestingExecutors`와
  bounded timeout으로 감싸며, 새로운 abstraction/dependency는 추가하지
  않았다.

## 이슈별 수용 증거

| 이슈 | 수용 기준 | 구현/테스트 증거 |
|---|---|---|
| #777 | retry reason에 raw exception message나 secret을 저장하지 않음 | `OutboxFailureReason`, redaction test |
| #781 | 같은 aggregate expected version의 동시 append 중 정확히 하나만 APPENDED | `event append CAS permits exactly one command at the same expected version` |
| #782 | route confirmation 동시성에서 정확히 하나 COMMITTED, blocking 기준 데이터 조회는 virtual thread 경계 | route confirmation 동시성 테스트와 기준 데이터 조회 virtual-thread boundary 테스트 |
| #784 | value-class ID 입력을 trim한 검증값으로 보존 | `value class identifiers preserve validated normalization` |

## 검증 영수증

모든 명령은 F1 worktree
`/Users/debop/work/bluetape4k/bluetape4k-workshop/.worktrees/fix/ecosystem-reuse-field-service-contracts`
에서 실행했다.

```text
RED: 4 targeted contract tests FAILED before implementation (expected)
GREEN: FieldServiceCasIntegrationTest — 7 passing
FULL: :optimization-field-service-dispatch:test — 56 passing, failures=0, errors=0, skipped=0
STATIC: ./gradlew detekt --rerun-tasks ... — BUILD SUCCESSFUL (106 actionable tasks)
DEPENDENCY: assertions/idgenerators/junit5 dependencyInsight — all BUILD SUCCESSFUL; selected 1.12.1 via bluetape4k-dependencies:1.4.0
DIFF: git diff --check — PASS
```

## 남은 게이트와 DoD

- 로컬 F1 implementation gate: PASS.
- manifest/path allowlist와 machine-readable run receipt: commit 후 갱신 필요.
- F1 exact head push, draft PR, hosted `Examples`/ecosystem gate, live review/thread
  read-back: PENDING.
- merge approval/merge/cleanup: 본 lane 범위 밖이며 fresh approval 전에는
  수행하지 않는다.

## DoD Status

Required checks: 7/7 local checks; N/A: 0; Blocked: 0; hosted CI/review/merge: PENDING.
